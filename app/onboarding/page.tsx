import { OnboardingExperience } from './OnboardingExperience';

/** /onboarding — 가입 직후 한 번. 생일(선택)과 QR 태그 등록(선택)을 묻는다. */
export default function OnboardingPage() {
  return <OnboardingExperience />;
}
